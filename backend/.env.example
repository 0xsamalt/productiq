# Copy to .env and fill in.

# Moss credentials (https://moss.dev dashboard)
MOSS_PROJECT_ID=your-moss-project-id
MOSS_PROJECT_KEY=your-moss-project-key

# HuggingFace Inference (https://huggingface.co/settings/tokens)
HF_TOKEN=hf_your_token_here
HF_LLM_MODEL=google/gemma-3-27b-it
# Speech-to-text (voice input) fallback model — served via provider="hf-inference"
HF_STT_MODEL=openai/whisper-large-v3
HF_INFERENCE_PROVIDER=auto

# App
APP_HOST=0.0.0.0
APP_PORT=8000
DB_URL=sqlite:///./productiq.db
STORAGE_DIR=./storage
