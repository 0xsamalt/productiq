fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
jinja2>=3.1.4
sqlmodel>=0.0.22
python-dotenv>=1.0.1

# Moss retrieval runtime
moss>=1.4.0

# HF Inference (Gemma 3 12B-IT)
huggingface_hub>=0.27.0

# PDF + text parsing
pypdf>=5.0.0

# PDF page → image rendering (used as Gemma-vision OCR fallback for scanned PDFs)
pymupdf>=1.24.0

# YouTube transcript ingestion (extracts auto-captions for video links)
youtube-transcript-api>=0.6.2

# HTML extraction for external doc links
beautifulsoup4>=4.12.0

# image handling
Pillow>=10.4.0

# nice-to-haves
pydantic>=2.7.0
httpx>=0.27.0
